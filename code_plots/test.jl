##
x = 1; y = 2;

println(x < y ? "less than" : "not less than")


x = 1; y = 0;

println(x < y ? "less than" : "not less than")

##TRY TO CREATE A FUNTION THAT RESEMBLES THE WATER COLUMN
Tᵢ(x, y, z) = z >= -SW_lim ? SW[2] :
            z >= -LIW_lim ? LIW[2] : DW[2] 
#


